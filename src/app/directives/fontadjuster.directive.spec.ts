import { FontadjusterDirective } from './fontadjuster.directive';

describe('FontadjusterDirective', () => {
  it('should create an instance', () => {
    const directive = new FontadjusterDirective();
    expect(directive).toBeTruthy();
  });
});
