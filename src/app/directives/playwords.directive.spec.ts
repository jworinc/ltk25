import { PlaywordsDirective } from './playwords.directive';

describe('PlaywordsDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaywordsDirective();
    expect(directive).toBeTruthy();
  });
});
