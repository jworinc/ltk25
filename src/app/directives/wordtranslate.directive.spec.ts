import { WordtranslateDirective } from './wordtranslate.directive';

describe('WordtranslateDirective', () => {
  it('should create an instance', () => {
    const directive = new WordtranslateDirective();
    expect(directive).toBeTruthy();
  });
});
