import { PlaysentenceDirective } from './playsentence.directive';

describe('PlaysentenceDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaysentenceDirective();
    expect(directive).toBeTruthy();
  });
});
